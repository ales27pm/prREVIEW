// extension/content.js

// This script is injected into GitHub pages as defined in manifest.json.
// Currently, all functionality is handled by the popup script (popup.js).
// This file could be used in the future to interact directly with the
// GitHub page DOM, for example, to add a "Run AI Review" button
// directly onto the pull request page.

console.log("PR Review Assistant content script loaded.");
