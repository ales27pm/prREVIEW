{
  "name": "pr-review-extension",
  "version": "0.1.0",
  "description": "Chrome extension for AI-powered PR reviews",
  "type": "module",
  "scripts": {
    "format": "prettier --write \"extension/**/*.{js,css,html,json}\"",
    "test": "prettier --check \"extension/**/*.{js,css,html,json}\" && NODE_OPTIONS=--experimental-vm-modules jest"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "p-limit": "^3.1.0",
    "prettier": "^2.8.8"
  }
}
